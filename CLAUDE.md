# Project guidance for Claude

## Dev server

Never start the dev server yourself. When a task needs a running server (visual checks, manual testing), ask the user to run `npm run dev` and report back. The user keeps the server running on their side.
